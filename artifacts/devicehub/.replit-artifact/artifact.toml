kind = "web"
previewPath = "/"
title = "DeviceHub"
description = "A minimal DeviceHub Flask status page that displays the German status message DeviceHub läuft. No database, authentication, or API included."
version = "1.0.0"
id = "artifacts/devicehub"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19211

[services.development]
run = "gunicorn --bind 0.0.0.0:19211 app:app"

[services.production]
run = "gunicorn --bind 0.0.0.0:19211 app:app"

[services.env]
PORT = "19211"
